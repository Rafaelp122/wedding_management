# Core dependencies for all environments
Django==5.2.9
django-allauth==65.13.1
django-htmx==1.27.0
djangorestframework==3.16.1

# Utilities
colorlog==6.10.1
Pillow==12.0.0
xhtml2pdf==0.2.17
qrcode[pil]==7.4.2

# Database & Caching
psycopg2-binary==2.9.10  # PostgreSQL adapter
redis==5.2.1  # Redis client

# Async & Task Queue
# Note: Celery and Redis are configured but not actively used in the current version.
# They are kept for future features (async emails, background reports, scheduled tasks).
celery==5.6.0  # Task queue
